# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Pinterest Discovery — generated files
data/pinterest-scored.json
data/pinterest-clusters.json

# Common ignores
node_modules
.next/
.DS_StoreSTATE.json
loop-run-log.md
.vercel
.env*
.env.local

# Standalone Go proxy — not part of the blog pipeline.
# Keep out of this repo to prevent keys from leaking on `git add .`.
claude-nvidia-proxy/

# IDX dev environment — contains API keys in dev.nix (hardcoded locally).
# Keep out of this repo to prevent keys from leaking on `git add .`.
.idx/
.worktrees

# Visual companion brainstorming (mockups éphémères)
.superpowers/

# Python bytecode residue
__pycache__/
