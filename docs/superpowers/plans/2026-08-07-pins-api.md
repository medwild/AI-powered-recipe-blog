# Endpoint pins `GET /api/recipes/pins` — Plan d'implémentation

> **Pour agentic workers:** SKILL REQUISE : utiliser `superpowers:subagent-driven-development` (recommandé) ou `superpowers:executing-plans` pour implémenter ce plan tâche par tâche. Les étapes utilisent la syntaxe checkbox (`- [ ]`).

**Goal:** Exposer `GET /api/recipes/pins` — liste JSON des recettes publiées (image, temps, tags, URL) pour l'app Google AI Studio qui génère des pins Pinterest.

**Architecture:** Un seul fichier `app/api/recipes/pins/route.ts` calqué sur le pattern existant de `app/api/recipes/raw/route.ts` : select Drizzle ciblé → mapping direct → `NextResponse.json`. Aucune dépendance ajoutée, aucun refactor adjacent.

**Tech Stack:** Next.js 16 App Router (route handler), Drizzle ORM (`drizzle-orm@^0.45.2`) sur Neon PostgreSQL, TypeScript.

## Global Constraints

- Filtrer `status = "published"` ET `content_type = "recipe"` — jamais d'article servi comme recette (règle globale n°2)
- Filtrer `heroImageUrl IS NOT NULL` — un pin sans image ne sert à rien
- Ne pas servir `category` (NULL en prod), ne pas inventer `calories`/`rating`/`chefTip`
- Temps en ISO 8601 bruts (`PT10M`) — pas de parsing
- URL absolue via `process.env.NEXT_PUBLIC_SITE_URL || "https://www.chefaugustin.com"` (pattern uniforme repo)
- 500 générique SANS `error.message` (règle sécurité)
- Pas de CORS, pas de handler OPTIONS (consommation serveur)
- Pas de `any`, pas de `@ts-ignore`, pas d'`eslint-disable` (règle n°9)
- Après modification : `npx tsc --noEmit` doit passer (CLAUDE.md)
- Commit final : message avec `Co-Authored-By: Claude <noreply@anthropic.com>`

---

### Task 1: Créer l'endpoint `GET /api/recipes/pins`

**Files:**
- Create: `app/api/recipes/pins/route.ts`
- Reference (pattern à suivre): `app/api/recipes/raw/route.ts` (ne PAS modifier)

**Interfaces:**
- Consumes: `db` de `@/lib/db`, table `recipes` de `@/lib/db/schema`, opérateurs `and`, `eq`, `desc`, `isNotNull` de `drizzle-orm`
- Produces: `GET /api/recipes/pins` → 200 `{ status, blogName, totalFound, syncedAt, recipes[] }`

- [ ] **Step 1: Vérifier le pattern existant**

Lire `app/api/recipes/raw/route.ts` en entier. Noter : `export const dynamic = "force-dynamic"`, le `where(and(eq(status…), eq(content_type…)))`, `.orderBy(desc(publishedAt))`, et le header `Cache-Control: public, max-age=3600, s-maxage=3600`.

- [ ] **Step 2: Créer `app/api/recipes/pins/route.ts`**

```typescript
import { NextResponse } from "next/server"
import { db } from "@/lib/db"
import { recipes } from "@/lib/db/schema"
import { and, eq, desc, isNotNull } from "drizzle-orm"

export const dynamic = "force-dynamic"

const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://www.chefaugustin.com"

export async function GET() {
  try {
    const all = await db
      .select({
        id: recipes.id,
        slug: recipes.slug,
        title: recipes.title,
        excerpt: recipes.excerpt,
        heroImageUrl: recipes.heroImageUrl,
        prepTime: recipes.prepTime,
        cookTime: recipes.cookTime,
        totalTime: recipes.totalTime,
        servings: recipes.servings,
        tags: recipes.tags,
        ingredients: recipes.ingredients,
        publishedAt: recipes.publishedAt,
      })
      .from(recipes)
      .where(
        and(
          eq(recipes.status, "published"),
          eq(recipes.content_type, "recipe"),
          isNotNull(recipes.heroImageUrl),
        ),
      )
      .orderBy(desc(recipes.publishedAt))

    const pins = all.map((recipe) => ({
      id: recipe.id,
      slug: recipe.slug,
      title: recipe.title,
      excerpt: recipe.excerpt,
      heroImageUrl: recipe.heroImageUrl,
      prepTime: recipe.prepTime,
      cookTime: recipe.cookTime,
      totalTime: recipe.totalTime,
      servings: recipe.servings,
      tags: recipe.tags,
      ingredients: recipe.ingredients,
      publishedAt: recipe.publishedAt?.toISOString(),
      url: `${SITE_URL}/recipes/${recipe.slug}`,
    }))

    return NextResponse.json(
      {
        status: "ok",
        blogName: "Chef Augustin",
        totalFound: pins.length,
        syncedAt: new Date().toISOString(),
        recipes: pins,
      },
      {
        headers: {
          "Cache-Control": "public, max-age=3600, s-maxage=3600",
        },
      },
    )
  } catch {
    return NextResponse.json(
      { error: "Erreur lors de la récupération des recettes" },
      { status: 500 },
    )
  }
}
```

Notes (à lire avant de copier) :
- Le `select` est **ciblé** (12 colonnes) — pas `getPublishedRecipesLight()` qui manque `excerpt`, `prepTime`, `cookTime`, `ingredients`
- Pas de `category` dans le select — NULL en prod, les `tags` portent la catégorisation
- `catch {}` sans paramètre — on n'expose jamais `error.message` (règle sécurité)
- Les temps restent en ISO 8601 bruts (`PT10M`) — le LLM les interprète
- Le `url` rejoint le pattern canonique de tout le repo (`app/sitemap.ts:11`, `app/robots.ts:3`, `app/layout.tsx:24`)

- [ ] **Step 3: Vérifier la compilation**

Run: `npx tsc --noEmit`
Expected: pas d'erreur (0 sortie). Si erreur de type, corriger le fichier — jamais ajouter `@ts-ignore`.

- [ ] **Step 4: Smoke test avec curl**

```bash
# Terminal 1 : lancer le serveur dev
npm run dev
# Terminal 2 : tester l'endpoint
curl -s localhost:3000/api/recipes/pins | node -e "
let s=''; process.stdin.on('data',d=>s+=d).on('end',()=>{
  const j = JSON.parse(s);
  console.log('status:', j.status);
  console.log('totalFound:', j.totalFound);
  console.log('sample:', JSON.stringify(j.recipes[0] && {slug:j.recipes[0].slug, url:j.recipes[0].url, prepTime:j.recipes[0].prepTime}));
})"
```

Expected:
- `status: "ok"`
- `totalFound: 46` (le nombre peut varier si de nouvelles recettes sont publiées — doit être > 0)
- `sample` : slug cohérent, `url` absolue `https://www.chefaugustin.com/recipes/{slug}`, `prepTime` en ISO 8601 (`PT…`)
- Aucune recette avec `category` dans la réponse

Vérifications manuelles complémentaires (si le serveur tourne) :
- `curl -s localhost:3000/api/recipes/raw | head -c 200` → l'endpoint existant répond toujours (pas de shadowing de route)
- `curl -s localhost:3000/api/recipes/pins | grep -c '"category"'` → 0 (le champ mort n'est pas servi)

- [ ] **Step 5: Commit**

```bash
git add app/api/recipes/pins/route.ts
git commit -m "$(cat <<'EOF'
feat(pins): endpoint GET /api/recipes/pins — recettes prêtes pour l'app Gemini

Liste des recettes publiées (46, avec image, temps ISO 8601, tags, URL
absolue) pour l'app Google AI Studio génératrice de pins Pinterest.
Calqué sur /api/recipes/raw : select ciblé 12 colonnes, filtre
content_type=recipe, Cache-Control public, 500 sans error.message.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review (fait à l'écriture)

**1. Couverture du spec :**
- §3 Architecture (select ciblé, filtres, force-dynamic, SITE_URL) → Task 1 Step 2 ✅
- §4 Contrat (shape, pas de category, ISO 8601, url absolue) → Step 2 mapping + Step 4 vérifs ✅
- §5 En-têtes (Cache-Control, pas de CORS) → Step 2 ✅
- §6 Erreurs (500 sans error.message) → Step 2 `catch {}` ✅
- §7 Vérification (tsc + curl : recipe only, image non-null, url) → Steps 3-4 ✅
- §8 Non-goals (pas de parsing, pas d'écriture, pas de dépendance) → aucune ligne ne les viole ✅

**2. Placeholder scan :** aucun TBD/TODO ; le code complet est inline ; les étapes de vérif sont exécutables telles quelles.

**3. Cohérence de types :** `db.select({...})` renvoie `heroImageUrl: string | null` — le filtre `isNotNull` ne change pas le type TS (le champ reste nullable côté type), mais `pins` le renvoie tel quel et le smoke test vérifie l'absence de null en pratique ; `publishedAt?.toISOString()` gère le nullable ; `isNotNull` est importé depuis `drizzle-orm` (vérifié exporté à l'exécution dans `node_modules/drizzle-orm`).
